spring:
  datasource:

  
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  
  flyway:
    enabled: true

logging:
  level:
    com.xinya: INFO
    org.springframework: WARN

server:
  port: ${SERVER_PORT:8080}
