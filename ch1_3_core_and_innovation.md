1.3　本文的核心内容与创新点

1.3.1　核心内容

本文围绕骨髓移植隔离病房的心理数字干预需求，完成了心芽 DTx 系统从需求分析到工程实现的全链路工作，主要内容涵盖以下三个方面。

第一，系统需求分析与总体架构设计。针对患者、医护人员和运维管理员三类用户角色，梳理功能需求与非功能需求，提出三端分层架构（Temi机器人终端、Spring Boot业务后端、Vue3运维管理平台）和双MySQL数据库隔离策略，设计通信协议与鉴权机制。

第二，服务端实现。在业务后端实现七个核心模块：基于五阶段临床路径（ADMISSION / PRETREATMENT / TRANSPLANT / REBUILD / DISCHARGE）的患者管理与阶段感知、每日幂等PRO症状上报、以Spring WebFlux实现的SSE流式AI对话代理（含超时降级兜底）、关键词热配置与AI情绪信号双层危机告警、希望树gamification激励、机器人设备绑定管理，以及JWT无状态鉴权与内部API Key双轨安全机制。在运维管理平台实现数据看板（ECharts可视化）、宣教内容管理、危机关键词与PRO题目热更新、用户管理与双库同步、AOP审计日志等管理功能。

第三，Temi机器人Android终端实现与系统集成测试。基于Jetpack Compose + Hilt + Room构建Local-First架构，实现AI对话（SSE流式逐字渲染）、每日PRO打卡（分步Wizard）、希望树可视化（Canvas动画）、冥想引导、宣教内容浏览及离线同步等功能模块；通过Temi SDK完成Kiosk锁定、语音识别接管与开机自启配置，将通用服务机器人改造为隔离病房专用终端。最后在三端联通条件下开展功能测试、端到端联调测试、SSE延迟测量与离线同步可靠性测试，对系统进行综合评估。

1.3.2　主要创新点

本文的主要创新点可归纳为以下五点。

（1）临床阶段感知的全链路DTx架构。将HSCT五阶段临床路径作为系统运行的核心驱动轴，PRO题目、宣教内容推送、希望树等级语义均与患者当前阶段状态深度绑定，实现了比通用DTx平台更高的场景针对性与临床适配性。

（2）Java网关SSE透传的低延迟设计。以Spring WebFlux作为AI服务的SSE代理网关，在Java层统一完成患者上下文组装、对话持久化与危机信号解析，同时将LLM推理解耦至独立Python服务，网关代理层引入的额外延迟仅约15ms，兼顾了流式交互体验与服务端业务逻辑的统一管控。

（3）Local-First加幂等批量同步机制。在资源受限的医疗IoT终端上，采用"操作优先写入本地Room数据库、网络恢复后批量同步"的架构，以clientTimestamp作为幂等键防止重复写入，在测试中达到100%的离线操作同步成功率，有效应对隔离病房偶发断网场景。

（4）Temi机器人专用化改造方案。通过AndroidManifest声明metadata_override_nlu = true屏蔽系统内置NLU、配置Kiosk独占前台控制、注册BootReceiver开机自启，将开放式商用服务机器人改造为符合隔离病房需求的专用医疗终端，为同类场景的机器人应用开发提供了可复用的工程参考。

（5）双层危机检测闭环。关键词热配置（运维端增删即时生效）与AI情绪信号（SSE done事件crisisAlert字段）两条路径并行触发危机告警，构成检测—持久化—运维看板展示—人工处置的完整生命周期，在不依赖WebSocket实时推送的前提下实现了可靠的危机响应机制。
