第二章　文献综述


2.1　数字疗法的技术范式与临床循证基础

数字疗法（Digital Therapeutics，DTx）是指由软件驱动、以循证医学证据为基础，面向患者提供预防、管理或治疗干预的医疗产品类别。与传统移动健康应用（mHealth）不同，DTx 须经过严格的临床试验验证并满足监管准入要求，其核心在于软件本身即为治疗手段，而非仅用于健康信息记录或行为提醒。2017 年美国 FDA 批准首款 DTx 产品 reSET（用于物质使用障碍）以来，全球 DTx 市场进入快速增长期，中国国家药品监督管理局亦于 2021 年将人工智能软件纳入第三类医疗器械审评框架，为 DTx 的本土化发展奠定了监管基础。

在肿瘤与血液病领域，DTx 的应用已从症状日记、依从性提醒扩展至认知行为干预（CBT）的数字化传递。Kim 等人的系统综述表明，基于移动应用的心理干预能够显著降低癌症患者的焦虑抑郁量表得分（PHQ-9 和 GAD-7），效应量在 0.3–0.6 之间，与人工心理咨询的短期效果相当[1]。对于骨髓移植（HSCT）患者，隔离环境使得面对面干预频率极低，DTx 的"永续在线、无接触"特性与该场景高度匹配：Mosher 等人的调查显示，HSCT 幸存者中约 40% 存在临床意义上的心理困扰，但仅 15% 接受过正式心理干预，可及性缺口显著[2]。

DTx 系统的核心架构要素包括：临床路径感知（根据患者所处治疗阶段动态调整干预内容）、患者自报告结局（Patient-Reported Outcomes，PRO）的持续采集、以及闭环反馈（将采集到的数据实时反馈至临床决策支持）。心芽 DTx 系统正是围绕上述三要素构建，以 HSCT 五阶段临床路径为主轴，以每日 PRO 打卡为数据采集手段，以希望树成长激励和危机告警为反馈机制。


2.2　大语言模型在临床对话场景中的应用

大语言模型（Large Language Models，LLMs）以 Transformer 架构为基础，通过在海量语料上进行预训练与指令微调，具备了开放域多轮对话、情感识别、知识检索与推理等能力。在心理健康领域，LLM 驱动的对话系统相比传统基于规则或检索的聊天机器人，在两个维度上取得了质的飞跃：其一，对话的自然度与上下文连贯性显著提升，能够在多轮交互中维持一致的治疗风格；其二，通过提示工程（Prompt Engineering）与检索增强生成（RAG），LLM 可被注入领域专业知识，使其在医学问答中保持较低的幻觉率。

Grodniewicz 等人将 GPT-4 与标准化患者访谈脚本进行比较，发现 LLM 在共情响应和反映性倾听等治疗性沟通维度上的评分与初级心理咨询师相当，尤其在处理焦虑和孤独感主题时表现稳定[3]。另一方面，LLM 的临床应用风险同样不容忽视：幻觉性医疗建议、对危机信号（如自杀意念）的误判或漏判，以及会话记录的隐私保护，均构成安全瓶颈。因此，当前主流做法是将 LLM 定位为"增强型辅助工具"而非独立临床决策者，配套建立关键词触发的危机告警机制和人工审核通道。

在架构层面，将 LLM 服务部署为独立的微服务（通常以 Python FastAPI 实现），由主应用后端充当 API 网关进行上下文组装与流量路由，已成为生产级 LLM 应用的主流模式。这一解耦设计使 LLM 服务可独立升级（如从 GPT-3.5 迁移至 GPT-4o），而不影响业务后端的稳定性。心芽 DTx 系统的心理智能体"小芽"与护理智能体"小护"均采用此模式，由 Spring Boot 业务后端负责患者上下文的组装（当前临床阶段、历史会话摘要）并代理转发至 Python FastAPI 智能体服务。


2.3　Server-Sent Events 与响应式流式传输

在大语言模型的对话接口中，由于完整回复的生成通常需要数秒，若等待全部 token 生成完毕后一次性返回，用户将面临"黑屏等待"的糟糕体验。流式传输（Streaming）技术将生成过程中的每个 token 或文本片段（delta）实时推送给客户端，使界面呈现逐字打字的视觉效果，将感知延迟从"等待完整回复"降低为"等待第一个字"。

实现流式推送的主流协议有两类：WebSocket（全双工，适合频繁双向交互）和 Server-Sent Events（SSE，半双工，服务器单向推送，基于标准 HTTP/1.1）。对于 LLM 对话场景，由于客户端仅需在发送消息后接收服务器的流式回复，并不需要双向并发通信，SSE 凭借其"零额外握手开销、可穿越 HTTP 代理、原生支持断线重连"等特性，成为 OpenAI API 等主流 LLM 接口的标准协议选择[4]。SSE 遵循 `text/event-stream` MIME 类型，每个事件以 `data:` 前缀开头，`\n\n` 双换行结尾，协议轻量且易于解析。

在服务端实现上，Spring WebFlux 提供了基于 Project Reactor 的响应式编程模型。`Flux<String>` 类型的返回值结合 `produces = MediaType.TEXT_EVENT_STREAM_VALUE` 注解，使 Spring MVC 控制器以最小代码量实现 SSE 响应流，同时通过背压（Backpressure）机制自动适应不同客户端的消费速率，避免缓冲区溢出[5]。相比传统阻塞式 Servlet 模型，WebFlux 在 SSE 长连接场景下的线程占用约为其 1/10，显著提升了高并发下的系统吞吐量。

在 Android 客户端，OkHttp 的 `EventSource` 接口与 Kotlin `callbackFlow` 的结合提供了将 SSE 回调异步事件桥接至 Kotlin 协程 `Flow` 的标准范式，`awaitClose` 语义确保协程取消时底层连接被正确释放，无需手动管理连接生命周期。


2.4　服务机器人平台在医疗场景中的部署与专用化改造

服务机器人（Service Robots）在医疗机构中的应用已从药品配送、消毒巡查等后勤任务，逐步向患者陪护、健康宣教和情感支持等高接触场景延伸。与专用医疗机器人（如手术机器人）不同，商业化通用服务机器人平台（如 Temi、Pepper、Kebbi）以低成本、可二次开发为优势，是当前医疗 DTx 研究中最常见的硬件载体。Cesta 等人综述了 2015—2022 年间住院场景机器人陪护研究，发现患者对机器人陪护的接受度随使用时长显著提升，主动交互频率在第 3 天后趋于稳定，表明适当的适应周期对提高使用效果至关重要[6]。

Temi 机器人（由 ADVANCE.AI 旗下 robotemi 公司推出）搭载高通骁龙处理器、10 英寸触控屏、360° 麦克风阵列及激光雷达自主导航系统，运行 Android 8.1，通过官方 SDK 向第三方开发者开放语音识别（ASR）、文本转语音（TTS）、自主导航、人脸检测及 Kiosk 锁定等接口。在医疗 DTx 场景的专用化改造中，有三个关键配置值得关注：

其一，Kiosk 模式（通过 `robot.requestToBeKioskApp()` 申请独占前台，配合 DeviceAdmin 策略防止用户退出应用）确保机器人始终处于可用状态，避免患者因误操作进入系统设置；其二，`metadata_override_nlu = true` Manifest 元数据配置屏蔽了 Temi 系统内置的唤醒词检测与意图识别（NLU），使应用可完整接收语音识别文本并由自定义 LLM 逻辑处理，避免"回主界面"等系统命令对患者对话的干扰；其三，`BootReceiver` 广播接收器实现开机自启，保障机器人在电源中断恢复后无需人工干预即可重新进入 DTx 界面。上述三重机制共同构成将通用服务机器人改造为隔离病房专用终端的工程基础。


2.5　移动端 Local-First 架构与离线优先数据同步

"Local-First 软件"的概念由 Kleppmann 等人于 2019 年系统提出，其核心主张是：用户数据首先存储于本地设备，网络同步为异步附加能力而非前提条件[7]。该架构范式尤其适合网络条件不稳定的场景（如医院隔离病房、地下室、弱覆盖区域），其优势包括：操作即时响应（写入本地延迟为毫秒级）、断网后功能完整保留、数据主权归属用户（服务端宕机不影响本地使用）。

在 Android 平台，Local-First 架构通常以 Room 数据库（SQLite ORM）作为本地数据层，结合 WorkManager 实现后台定时同步任务。Room 的 `Flow<T>` 响应式查询接口与 Jetpack Compose 的 `collectAsStateWithLifecycle()` 形成天然对接——数据库变化自动触发 UI 重组，无需手动轮询，完全符合响应式编程范式。WorkManager 提供约束驱动的任务调度（如 `NetworkType.CONNECTED` 约束），在网络恢复时触发批量同步，同时支持指数退避重试策略，保障高负载下的幂等性[8]。

服务端幂等性设计是 Local-First 同步的关键挑战：客户端可能在网络中断后多次重传同一操作，服务端须能识别并静默丢弃重复请求。常见方案是在每条操作记录中携带客户端生成的唯一标识（如 `clientTimestamp` 或 UUID），服务端维护已处理 ID 集合。此外，Jetpack Compose 声明式 UI 框架以 `StateFlow` 为状态容器，通过智能重组（Smart Recomposition）仅重绘发生变化的组件树节点，在逐字渲染 LLM 流式输出时尤为高效，相比传统 View 系统的 `invalidate()/onDraw()` 循环具有更低的 CPU 和 GPU 开销。

综合以上技术综述，心芽 DTx 系统的设计选型具有充分的文献依据：以 DTx 框架定义系统边界与临床目标，以 LLM 微服务提供核心对话能力，以 SSE/Spring WebFlux 实现低延迟流式体验，以 Temi SDK 专用化改造提供可靠的机器人硬件接入，以 Local-First + Room + WorkManager 保障隔离病房弱网环境下的数据完整性。各技术组件的组合并非随意堆砌，而是由隔离病房这一特殊临床场景的约束条件共同驱动。
