<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xinya.business.agent.mapper.ConversationMapper">

    <select id="findByPatientIdAndSessionId" resultType="com.xinya.business.agent.entity.Conversation">
        SELECT * FROM conversations
        WHERE patient_id = #{patientId} AND session_id = #{sessionId}
        ORDER BY created_at DESC
    </select>

    <select id="findByPatientIdAndAgentTypeOrderByCreatedAtDesc" resultType="com.xinya.business.agent.entity.Conversation">
        SELECT * FROM conversations
        WHERE patient_id = #{patientId} AND agent_type = #{agentType}
        ORDER BY created_at DESC
    </select>

    <select id="findByPatientIdOrderByCreatedAtDesc" resultType="com.xinya.business.agent.entity.Conversation">
        SELECT * FROM conversations
        WHERE patient_id = #{patientId}
        ORDER BY created_at DESC
    </select>

</mapper>
