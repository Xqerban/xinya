<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xinya.business.pro.mapper.ProRecordMapper">

    <select id="findByPatientIdAndDateRangeOrderByDesc" resultType="com.xinya.business.pro.entity.ProRecord">
        SELECT * FROM pro_records
        WHERE patient_id = #{patientId}
          AND record_date BETWEEN #{start} AND #{end}
        ORDER BY record_date DESC, created_at DESC
    </select>

</mapper>
