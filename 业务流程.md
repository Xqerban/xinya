## 业务流程图

### 1. 系统整体业务流程

```mermaid
flowchart TD
    A([患者入仓]) --> B[护士创建患者档案\\nPAD端]
    B --> C[系统初始化\n·希望之树 Lv.1\n·心理能量 50\n·临床阶段: ADMISSION]

    C --> D{机器人/PAD是否在线?}
    D -- 在线 --> E[数据实时同步至云端]
    D -- 离线 --> F[数据写入本地Room DB\n加入同步队列]
    F --> G[网络恢复后\n/api/sync/batch 批量上传]
    G --> E

    E --> H[进入每日循环]

    subgraph 每日交互循环
        H --> I[早间: 心理陪护 小芽\n情绪签到 + CBT引导]
        I --> J[午间: 每日PRO症状打卡]
        J --> K[下午: 护理宣教伙伴推送\n阶段匹配的宣教内容]
        K --> L[晚间: 希望之树成长动画\n展示今日积累成就]
        L --> M{检测到危机信号?}
        M -- 是 --> N[触发危机预警\n推送至医护PAD]
        N --> O[医护介入处理]
        O --> H
        M -- 否 --> P{是否阶段流转时机?}
        P -- 是 --> Q[PAD端执行阶段推进\n/api/clinical/transition]
        Q --> H
        P -- 否 --> H
    end

    H --> R{达到出仓条件?}
    R -- 否 --> H
    R -- 是 --> S[阶段流转至 DISCHARGE]
    S --> T[生成患者康复报告]
    T --> U([患者出仓])
```

---

### 2. 临床五阶段路径流转

```mermaid
stateDiagram-v2
    direction LR
    [*] --> ADMISSION : 入仓登记

    ADMISSION --> PRETREATMENT : 医护确认预处理开始
    note right of ADMISSION
        · 环境适应宣教
        · 基础情绪评估
        · 希望之树初始化
    end note

    PRETREATMENT --> TRANSPLANT : 预处理方案完成
    note right of PRETREATMENT
        · 化疗/放疗副作用管理
        · 症状PRO高频打卡
        · CBT焦虑干预
    end note

    TRANSPLANT --> REBUILD : 干细胞回输完成
    note right of TRANSPLANT
        · 移植日仪式感设计
        · 感染风险宣教
        · 情绪支撑高峰期
    end note

    REBUILD --> DISCHARGE : 血象重建达标
    note right of REBUILD
        · 造血重建监测
        · 排异反应知识推送
        · 自我护理赋能
    end note

    DISCHARGE --> [*] : 出院
    note right of DISCHARGE
        · 出院宣教
        · 随访计划
        · 康复报告生成
    end note

    PRETREATMENT --> ADMISSION : 回退（异常）
    TRANSPLANT --> PRETREATMENT : 回退（异常）
    REBUILD --> TRANSPLANT : 回退（异常）
```

---

### 3. 患者端（机器人）每日交互流程

```mermaid
sequenceDiagram
    actor 患者
    participant Robot as 机器人端
    participant DB as 本地Room DB
    participant API as 后端 API
    participant Agent as AI智能体

    患者->>Robot: 唤醒机器人
    Robot->>DB: 读取患者本地状态
    Robot-->>患者: 展示今日希望之树 & 打招呼

    患者->>Robot: 开始情绪对话
    Robot->>API: POST /api/agent/chat (agentType=psych)
    API->>Agent: 转发至心理陪护智能体
    Agent-->>API: 回复 + psychEnergyDelta + crisisAlert
    API-->>Robot: AgentChatResponse
    Robot-->>患者: 语音/文字回复

    alt 检测到危机信号
        API->>API: 创建危机预警记录
        API-->>Robot: crisisAlert: true
        Robot-->>患者: 安抚话语 + 呼叫护士
    end

    患者->>Robot: 进行每日打卡
    Robot->>API: POST /api/pro/submit
    API-->>Robot: 打卡成功 + psychEnergyDelta +10
    Robot->>API: POST /api/hopetree/grow (growthSource=check_in)
    API-->>Robot: 希望之树成长动画数据

    Robot-->>患者: 展示树的生长动画 🌱

    Note over Robot,DB: 若离线，数据先存本地
    Note over DB,API: 网络恢复后自动批量同步
```

---

### 4. 医护端（PAD）操作流程

```mermaid
flowchart LR
    A([医护登录\nPAD端]) --> B[查看数据驾驶舱\nGET /api/dashboard/overview]

    B --> C{有未处理预警?}
    C -- 是 --> D[查看预警详情\nGET /api/alerts]
    D --> E[进入患者详情\nGET /api/patients/id]
    E --> F[查看心理能量趋势\n& 症状打卡历史]
    F --> G[处理预警\nPUT /api/alerts/id/resolve]
    G --> B

    C -- 否 --> H[查看患者列表\nGET /api/patients]
    H --> I[选择患者]
    I --> J{需要阶段流转?}
    J -- 是 --> K[执行阶段推进\nPOST /api/clinical/transition]
    K --> B

    J -- 否 --> L{需要内容推送?}
    L -- 是 --> M[查看宣教内容库\nGET /api/education/contents]
    M --> N[关联推送至患者]
    N --> B

    L -- 否 --> B
```

---

### 5. 危机干预流程

```mermaid
flowchart TD
    A[患者发送对话消息] --> B[后端危机关键词检测]
    B --> C{命中危机词?}

    C -- 否 --> D[正常对话流程返回]

    C -- 是 --> E[crisisAlert = true]
    E --> F[写入 crisis_alerts 表]
    F --> G[实时推送 WebSocket\n通知医护PAD]
    G --> H[机器人端展示安抚内容\n+ 呼叫护士按钮]
    H --> I{医护30分钟内响应?}

    I -- 响应 --> J[医护标记处理中\nPUT /api/alerts/id/resolve]
    J --> K[记录干预结果]
    K --> L([结束])

    I -- 未响应 --> M[系统升级预警等级\nalert.level = critical]
    M --> N[短信通知值班医生]
    N --> J
```

---

### 6. 离线数据同步流程

```mermaid
sequenceDiagram
    participant App as Android端
    participant Queue as 本地同步队列\n(SyncQueueEntity)
    participant Worker as SyncManager\n(WorkManager)
    participant API as 后端 /api/sync/batch

    Note over App: 离线状态

    App->>Queue: 所有写操作入队\n(type, payload, createdAt)
    App->>Queue: 多条记录累积...

    Note over Worker: 网络恢复 / 15分钟定时触发

    Worker->>Queue: 读取所有 status=PENDING 记录
    Queue-->>Worker: List<SyncQueueItem>

    Worker->>API: POST /api/sync/batch\n{ items: [...] }

    API->>API: 幂等处理每条记录\n(按clientId去重)
    API-->>Worker: SyncBatchResponse\n{ succeeded: [], failed: [] }

    Worker->>Queue: 更新成功记录 status=SYNCED
    Worker->>Queue: 失败记录 retryCount++\n超过3次标记 status=FAILED

    Note over App: 同步完成，数据一致
```

---

## 三端架构概览

```
┌──────────────────────────────────────────────────────────────────┐
│                        三端功能分区                               │
├──────────────────┬───────────────────┬───────────────────────────┤
│   患者端（机器人）│   医护端（PAD）    │   运维端（Web后台）        │
│   Android Kotlin  │   Android/Web Vue │   Vue3 + Element Plus     │
├──────────────────┼───────────────────┼───────────────────────────┤
│ · 情绪对话（小芽）│ · 数据驾驶舱       │ · 宣教内容管理（CRUD）     │
│ · 护理宣教推送   │ · 患者列表总览     │ · 患者批量导入/导出        │
│ · PRO每日打卡    │ · 阶段流转操作     │ · 系统配置管理             │
│ · 希望之树动画   │ · 危机预警处理     │ · 数据统计报表             │
│ · 冥想/呼吸练习  │ · 打卡历史查看     │ · 用户权限管理             │
│ · 离线优先架构   │ · 推送内容给患者   │ · 审计日志查询             │
└──────────────────┴───────────────────┴───────────────────────────┘
                            ↕ HTTP REST API
┌──────────────────────────────────────────────────────────────────┐
│              Spring Boot 后端 (localhost:8080)                    │
│  MySQL 8.x — xinya_dtx 数据库                                    │
└──────────────────────────────────────────────────────────────────┘
                            ↕ HTTP / SDK
┌──────────────────────────────────────────────────────────────────┐
│             Python AI 智能体（独立部署，由专人对接）              │
│   小芽-心理（CBT + 危机检测）  ·  小护士-护理（阶段知识推送）    │
└──────────────────────────────────────────────────────────────────┘
```

---